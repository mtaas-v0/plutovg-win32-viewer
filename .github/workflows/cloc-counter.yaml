name: Manual SLOC Count

on:
  workflow_dispatch:

jobs:
  count-sloc:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install and run cloc
        run: |
          sudo apt-get update && sudo apt-get install -y cloc
          cloc .
